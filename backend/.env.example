# Copy to backend/.env and fill in. Loaded by pipeline/download.py; real env vars always win.
# Override the file path with POSEIDON_ENV_FILE if backend/.env isn't where you want it.
COPERNICUSMARINE_SERVICE_USERNAME=
COPERNICUSMARINE_SERVICE_PASSWORD=
EARTHDATA_USERNAME=
EARTHDATA_PASSWORD=
